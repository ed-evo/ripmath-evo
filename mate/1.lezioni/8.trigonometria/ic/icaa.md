# Formule di addizione e sottrazione

Le formule di addizione e sottrazione (in breve formule di addizione) sono le formule principali da cui si ricavano tutte le altre: è una delle poche cose in matematica che (forse) sarebbe bene studiare a memoria

- $\cos(\textcolor{blue}{\alpha} - \textcolor{blue}{\beta})$
- $\cos(\textcolor{blue}{\alpha} + \textcolor{blue}{\beta})$
- $\operatorname{sen}(\textcolor{blue}{\alpha} + \textcolor{blue}{\beta})$
- $\operatorname{sen}(\textcolor{blue}{\alpha} - \textcolor{blue}{\beta})$
- tabella di riepilogo
- formule per la tangente
- uso delle formule