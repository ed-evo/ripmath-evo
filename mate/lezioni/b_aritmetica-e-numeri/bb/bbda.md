$$
\textcolor{blue}{(+3)} \cdot \textcolor{blue}{(+2)} =
$$

È il caso che deve corrispondere al prodotto fra naturali
considero il primo numero su $Z$

Per moltiplicare per $+2$ ripeto due volte il numero dalla stessa parte dove si trova

$$
\textcolor{blue}{(+3)} \cdot \textcolor{blue}{(+2)} = +6
$$